[includeIf "gitdir:~/.dotfiles"]
	# Proxemy user.name and .email
	path = ~/.gitconfig_Proxemy
[core]
	editor = nvim
[alias]
	l = log --graph --oneline --relative-date --pretty='%C(yellow)%h %Cblue%>(12)%ad %Cgreen%<(7)%aN%Cred%d %Creset%s'
	s = status -sb --ahead-behind
	d = diff --minimal
	co = checkout
[init]
	defaultBranch = main

# different remote locations for pull/push
# unfortunately infers with other git invocations besides the dotfiles repo
#[remote "origin"]
	#url = https://github.com/proxemy/dotfiles
	#pushurl = git@github.com:proxemy/dotfiles.git
